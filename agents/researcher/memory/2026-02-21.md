# 2026-02-21 - Daily Notes

## 模型配置更改

### 问题诊断
用户发现之前配置的 `zai/glm-4.7` 调用的是第三方 Z.AI 平台，而不是他购买的智谱AI开放平台（bigmodel.cn）包年套餐。

### 配置修改
- 修改了 `~/.openclaw/openclaw.json`
- 添加环境变量：`BIGMODEL_API_KEY` = `63c8b255a3fd48168a8dc5329f27c41c.jPgGCTA1yX1u9eEa`
- 添加自定义提供商 `bigmodel`：
  - baseUrl: `https://open.bigmodel.cn/api/paas/v4`
  - api: `openai-completions`
- 所有 agent 的模型从 `zai/glm-4.7` 改为 `bigmodel/glm-4.7`

注意：配置文件后来被用户手动修改，provider 名称变成了 `glm`。

### 尝试设置默认 agent
用户尝试将默认 agent 设置为"总指挥"（zongzhihui），但出现错误，手动改回去了。

## 智谱AI MCP 服务

用户提到智谱AI提供 MCP web-reader 服务：
- URL: `https://open.bigmodel.cn/api/mcp/web_reader/mcp`
- 功能：抓取网页内容
- 说明：OpenClaw 不支持标准 MCP 协议，已使用内置 `web_fetch` 工具替代

## 用户信息

- 使用智谱AI开放平台包年套餐
- 平台：https://open.bigmodel.cn
- API Key 已记录（见上）
